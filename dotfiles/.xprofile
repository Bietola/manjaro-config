# Vim is the wea
setxkbmap -option caps:escape

# Desktop notifications
# TODO: avoid having to kill `notify-osd` at every startup
dunst &

# Anything at the press of a key
# TODO: find a way of using a systemd service
sxhkd &

# Blue light is evil
redshift -O 3000

# Start clipboard history manager daemon
anamnesis --start

# Evergreen trees are a staple at this point
feh --bg-fill ~/sync/img/desktops/evergreen-tre.jpg
